#include "addition.h" // our own libraries


int main(){ // entry point in C
    int first_value, second_value; // variable declaration
    first_value = 55;
    second_value = 15;
    addition(first_value, second_value); // cntrl + l --> to clear terminal
    addition(40, 10);
}
